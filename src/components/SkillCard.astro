---
export interface Props {
  icon: string;
  skill: string;
}

const { icon, skill } = Astro.props;
---

<li class='w-20 h-28 py-4 rounded-lg bg-c-light-blue shadow-lg flex flex-col items-center sm:h-44 sm:w-36 sm:justify-around md:h-52 md:w-48'>
    <img class='h-2/4' src={icon} alt={skill}>
  <div class='mt-4 sm:text-xl text-zinc-50'>
    {skill}
  </div>
</li>
